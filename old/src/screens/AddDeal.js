import { View, Text } from "react-native";

// ? Page where the business can add in a deal

const AddDeal = () => {
    return (
        <View>
            <Text>Add Deal</Text>
        </View>
    );
};

export default AddDeal;
