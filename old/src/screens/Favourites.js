import { Text, View } from "react-native";

// ? Can be a favourites section
// !!! THis will come later when we add in user signing up, first version wont have that

const Favourites = () => {
    return (
        <View>
            <Text>Favourites</Text>
        </View>
    );
};

export default Favourites;
