import { View, Text } from "react-native";

// ? screen to show info on a business, their deals and events, clicking on the deal from the deals list will go here but just automatically open up their deal as well

const Business = () => {
    return (
        <View>
            <Text>Business</Text>
        </View>
    );
};

export default Business;
