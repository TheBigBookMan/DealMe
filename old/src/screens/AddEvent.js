import { View, Text } from "react-native";

// ? Page where business can add in an event

const AddEvent = () => {
    return (
        <View>
            <Text>Add Event</Text>
        </View>
    );
};

export default AddEvent;
