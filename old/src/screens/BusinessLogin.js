import { View, Text } from "react-native";

// ? The business login page- need to have a usercontext for this or whatever react native does

const BusinessLogin = () => {
    return (
        <View>
            <Text>Login</Text>
        </View>
    );
};

export default BusinessLogin;
