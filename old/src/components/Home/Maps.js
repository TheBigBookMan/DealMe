import { Text, View } from "react-native";

// ? Have a smaller google maps which has some like smaller icons that show nearby things are on and user clicks on it and it goes to the maps page
// ? A map with nearby locations with maybe little red dots on them on the map close by where something is on and maybe yellow dots fr favourites places that have things on for that night
const Maps = () => {
    return (
        <View>
            <Text className="text-white">Mapos</Text>
        </View>
    );
};

export default Maps;
