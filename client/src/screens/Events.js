import { View, Text } from "react-native";

// ? Can be a seperate page for more specific events, maybe a later version of the app

const Events = () => {
    return (
        <View>
            <Text>Events</Text>
        </View>
    );
};

export default Events;
