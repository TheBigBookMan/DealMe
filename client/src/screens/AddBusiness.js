import { View, Text } from "react-native";

// ? Where a user can add in information about their business to advertise the deals
// ? Not sure how to make it reliable with real businesses, andy will need to look into that

const AddBusiness = () => {
    return (
        <View>
            <Text>Add Business</Text>
        </View>
    );
};

export default AddBusiness;
