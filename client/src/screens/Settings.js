import { View, Text } from "react-native";

// ? Where a user can put settings such as- location setting
// ? User can add in they want to add a business to it

const Settings = () => {
    return (
        <View>
            <Text>Settings</Text>
        </View>
    );
};

export default Settings;
